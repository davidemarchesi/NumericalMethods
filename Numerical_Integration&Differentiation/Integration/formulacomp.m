function I = formulacomp(a, b, N, fun)
% 
% Inputs:
%      a,b: estremi di integrazione,
%      N: numero di sottointervalli (m=1 formula di integrazione semplice)
%      fun: funzione
% Output:
%      I: integrale calcolato

h = % ampiezza sottointervalli

x =  % estremi/punti medi degi sottointervalli

fx = % valuto i punti x con fun

I = % calcolo integrale approssimato
    
end